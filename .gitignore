# Secrets — never commit real credentials
.env
*.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/

# Uploads / local data
uploads/
dataset/raw_documents/
dataset/preprocessed/

# OS / editors
.DS_Store
Thumbs.db
.vscode/

# Local tool configuration is ignored because it may contain environment-
# expanded MCP credentials. The checked-in example must contain placeholders.
opencode.json
opencode.jsonc
tessdata/*.traineddata

# Demo fixture document is tracked; regenerate the derived JSON with
# scripts/build_demo_fixture.py if it is ever missing.

# Local/unrelated files
Videos/

# Local/generated tooling
.vite/
.codex/stitch-reference/
.codex/stitch-site-one-2/
.codex/stitch-source/
